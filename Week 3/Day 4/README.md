# 🚀 Week 3 - Day 4: PySpark DataFrame Transformations

## 📌 Objective

Practice DataFrame transformations, column operations, aliasing, and filtering using PySpark.

## 🛠️ Technologies Used

* Python
* PySpark
* Databricks

## 📚 Concepts Covered

* DataFrame Creation
* Column Selection (`select`)
* Column Aliasing (`alias`)
* Filtering Rows (`filter`)
* Conditional Filtering
* Range Filtering (`between`)
* Logical Operators (`&`, `|`)
* String Filters (`startswith`, `endswith`, `contains`)
* Dropping Columns (`drop`)
* Using `col()` Function

## ✅ Exercises Completed

* Created DataFrames from sample data
* Selected single and multiple columns
* Renamed columns using aliases
* Filtered records by salary, age, city, and department
* Applied AND & OR conditions
* Used `between()` for range filtering
* Used `startswith()`, `endswith()`, and `contains()`
* Removed unwanted columns using `drop()`
* Created customized DataFrame outputs

## 🎯 Key Learnings

* Working with DataFrame transformations
* Applying column-level operations
* Filtering data with multiple conditions
* Using aliases for better readability
* Performing string-based filtering
* Building reusable PySpark queries

## 📊 Outcome

Successfully completed 30+ PySpark DataFrame transformation and filtering exercises, strengthening core Data Engineering and data processing skills.
